#Password
#Last Modified: June 12th, 2024
#Author: Bhavan 
#Desc: user enters a password and it's compared to a password of my choice. If it is correct, it will display a secret message.  If it is incorrect, it will tell the user they are incorrect.

#INPUT&PROCESSING: making password and user input 
password = 'abc123'
passwordUser = (input("Enter a password:"))

#PROCESSING: check if input is correct
while passwordUser != password:
    #OUTPUT: password is wrong
                 passwordUser = (input("Wrong. Enter a password:"))
#PROCESSING: check if password is correct
if passwordUser == password:
#OUTPUT: password is right
                                 print("Good job! the password was", password, "!")
